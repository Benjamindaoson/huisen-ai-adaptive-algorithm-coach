## Why\n\nThe product can already arrange a local adaptive training path from validated learning events, while the deeper Mentor runtime needs a configured gateway. Today these two layers are collapsed into one status. A learner can see `AI 已记录 3 个入口动作` beside `导师暂不可用`, which makes the intelligence feel fake even though both statements come from honest but different capabilities.\n\n## What Changes\n\n- Name the always-available local evidence planner separately from the deep code Mentor.\n- In local-only or failed-connection states, disclose that no model or dynamic tool ran while preserving the truth that local adaptive planning still works.\n- Use the current route contribution's bounded `nextAction` as the dock fallback before a runtime checkpoint exists.\n- Preserve all existing remote Mentor lifecycle, recovery, approval, isolation, and assessment-suppression behavior.\n\n## Capabilities\n\n### New Capabilities\n\n- `truthful-mentor-availability`: capability-layer status and route-scoped local fallback action.\n\n### Modified Capabilities\n\n- None.\n\n## Impact\n\n- `web/src/components/MentorDock.tsx` and its component contracts.\n- `web/src/components/AppShell.tsx` and its navigation/status contract.\n- No API, persistence, event, model, or route schema changes.\n\n