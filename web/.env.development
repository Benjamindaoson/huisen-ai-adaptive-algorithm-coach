# Loopback-only public endpoints for the repository's local development stack.
# Secrets stay in services/runner/.env and must never use a VITE_ prefix.
VITE_PLATFORM_API_URL=http://127.0.0.1:8787
VITE_LEARNING_API_URL=http://127.0.0.1:8787
VITE_RUNNER_URL=http://127.0.0.1:8787
